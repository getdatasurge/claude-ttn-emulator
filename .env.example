# FrostGuard LoRaWAN Emulator - Environment Variables
# Copy this file to .env.development (for local dev) or configure in Netlify dashboard

# Stack Auth Configuration (Required)
# Get these from https://app.stack-auth.com
VITE_STACK_PROJECT_ID=your-stack-project-id
VITE_STACK_PUBLISHABLE_CLIENT_KEY=your-stack-publishable-key

# Cloudflare Workers API (Required for full functionality)
VITE_API_BASE_URL=https://your-worker.your-subdomain.workers.dev

# Turso Database Configuration (Optional - for direct DB access)
VITE_TURSO_DATABASE_URL=libsql://your-database.turso.io
VITE_TURSO_AUTH_TOKEN=your-turso-auth-token

# Feature Flags (Optional)
VITE_ENABLE_DEBUG_LOGS=false
VITE_ENABLE_PERFORMANCE_MONITORING=false

# ============================================
# For Netlify: Add these in Site Settings >
# Build & deploy > Environment variables
# ============================================
